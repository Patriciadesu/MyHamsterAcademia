{
  "name": "myhamsteracademia",
  "version": "1.0.0",
  "scripts": {
    "dev": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop ecosystem.config.js",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "pm2": "^7.0.1"
  }
}
